version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm ci
  build:
    commands:
      - npm test
      - echo "Build completed"
artifacts:
  files:
    - '**/*'

