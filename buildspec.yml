version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 24
    commands:
      - npm ci
  build:
    commands:
      - npm test
      - echo "Build completed"
artifacts:
  files:
    - '**/*'

